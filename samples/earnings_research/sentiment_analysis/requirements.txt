# AWS
boto3>=1.34.0
# data
pandas>=2.0.0
polars>=1.0.0
pyarrow>=15.0.0
numpy>=1.26.0
# direct-to-S3 io for polars csv + binary artifacts.
# s3fs pins an EXACT matching fsspec release, and some releases pin yanked
# versions (s3fs 2025.3.x -> fsspec==2025.3.1, yanked from PyPI). Pin the
# pair to the version verified locally.
s3fs==2026.7.0
fsspec==2026.7.0
# trading calendar (trade-day mapping)
pandas_market_calendars>=4.4.0
# report charts
matplotlib>=3.8.0
