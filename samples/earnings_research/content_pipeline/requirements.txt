# AWS access
boto3>=1.34.0

# DataFrames + parquet (Polars-native pipeline)
polars>=1.0.0
pyarrow>=15.0.0

# Async HTTP fetching
aiohttp>=3.9.0

# Text extraction
pypdf>=4.0.0
trafilatura>=1.8.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Headed-browser backfill for bot-walled hosts (needs `playwright install
# chromium` + a virtual display / xvfb on servers)
playwright>=1.40.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-aiohttp>=1.0.0
hypothesis>=6.0.0
moto[s3]>=5.0.0
